// Gets the first element of array.

// ++++ARGUMENT++++
// array(Array): The array to query.

function head(array) {
  return array[0];
}

// +++RETURN+++
// (*): Returns the first element of array./


// +++EXAMPLE+++
// head([1, 2, 3]);
// => 1
// head([]);
// => undefined